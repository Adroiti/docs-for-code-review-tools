Pattern: Use of blacklisted built-in function

Issue: -

## Description

Usual blacklisted functions are the ones like `map` or `filter`, where Python now offers some cleaner alternative like list comprehension.
