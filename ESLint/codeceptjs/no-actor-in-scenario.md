Pattern: Use of `actor` in `Scenario`

Issue: -

## Description

Prevents the use of the `actor` in a `Scenario` and delegate to page objects.

## Further Reading

* [eslint-plugin-codeceptjs](https://github.com/poenneby/eslint-plugin-codeceptjs#supported-rules)