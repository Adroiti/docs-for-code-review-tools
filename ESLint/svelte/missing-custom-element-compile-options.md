Pattern: Missing compile options for custom element

Issue: -

## Description

The `tag` option is used when generating a custom element. Did you forget the `customElement: true` compile option?